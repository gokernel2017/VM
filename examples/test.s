
int i, a = 10, b = 20, c = 3, d = 5, x;

function hello(i) {
  "\nFunction Hello\n";
}

  a * b + c * d; // display: 215

  if (a < b && !i) { // true

      "\nVariable a: " a; "\n";

  }

  info (1); // display variable list:

  help; // call a function WhitOut Argument
